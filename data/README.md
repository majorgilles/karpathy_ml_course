# Data

Shared data used across lessons.

- `raw/` — source datasets such as `names.txt` or Shakespeare text before transformation.
- `processed/` — derived datasets that are small enough and useful to keep.
- `external/` — third-party reference files or papers used by exercises.

Prefer committing small, reproducible datasets. Keep large generated files and model checkpoints out of git.
